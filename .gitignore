# Dependencias
node_modules/

# Variables de entorno (secretos) — se versiona solo .env.example
.env
.env.local
*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Hardhat (artefactos y cache de compilacion)
artifacts/
cache/
typechain-types/

# Info de los contratos generada al desplegar (especifica del entorno)
src/blockchain/contract-info.json
src/blockchain/nft-info.json

# Archivos subidos y metadata generada en tiempo de ejecucion
uploads/*
!uploads/.gitkeep
metadata/*
!metadata/.gitkeep
pdfs/*
!pdfs/.gitkeep

# Editor / herramientas
.idea/
.vscode/
.DS_Store
Thumbs.db

# Archivos temporales de Office (locks ~$...) y de prueba
~$*
*.tmp
banner-test.png
